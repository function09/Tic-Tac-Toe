const gameBoard = (() => {
  const gameBoardArray = [""];

  selectSquares.forEach((square) => {
    square.addEventListener("click", () => {
      console.log();
    });
  });
})();

const playerFactory = () => {};

const gameFlow = (() => {})();
